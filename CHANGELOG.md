# 更新日志

本文件按版本记录变更。发布时 `release.yml` 的 publish 步骤会自动把对应段落写入
GitHub Release 说明（release notes），因此每次发版前请先在这里补一段。

格式：`## vX.Y.Z (YYYY-MM-DD)`，段落之间用下一个 `## ` 标题分隔。

## v0.18.1 (2026-09-26)

- **修复传输指示永久卡住**：打开远程文件（先下载到临时目录再打开）或完成 SFTP/HTTP 下载、上传后，状态栏指示器停在「下载中… 100%」不消失。根因是前端只在收到 `done=true` 的收尾事件后 1.5 秒才清除指示，而 SFTP `download/download_to/upload` 三个命令结束时从不发收尾事件，HTTP 下载的收尾事件也漏置 `done` 标志；现全部补齐（失败同样收尾，指示器不再悬挂）。
- **修复传输文案重复**：状态栏拼接格式为「下载中：〈文件名〉」，但 SFTP/HTTP 传输把文件名硬编码成了固定文案，渲染成「下载中：下载中…」；现 SFTP 下载/上传与 HTTP 下载均传实际文件名（能看到正在传输哪个文件），复制/移动等不逐文件改名的任务由状态栏做通用文案去重。

## v0.18.0 (2026-09-26)

- **同步比对（底部实时面板）+ 同步浏览**：工具栏 / 菜单「工具 → 同步比对（左右窗格）」/ 窗格右键开启。要求当前标签恰好两个路径窗格（本地↔本地、本地↔SFTP、SFTP↔SFTP；HTTP 索引与标签视图不参与）。
  - **同步浏览**：开启时两侧目录成为映射锚点，一侧进入子目录另一侧自动进入同名相对子路径（覆盖地址栏/侧栏/前进后退/双击定位等所有导航入口）；对侧无同名目录时停在原地，面板横幅提示「两侧不同层」，支持 **[在对侧新建并进入]**（唯一远程写入点，逐级创建）/ **[返回对齐]** / **[断开链接]**，回到共同祖先自动重对齐。
  - **实时比对**：面板随导航防抖 120ms 重算**当前层**（名称/类型 + 大小，非递归），在途任务自动取消、过期响应丢弃；头部显示对齐状态与两侧路径，支持只看差异、交换左右、重新对齐、折叠；行列表展示名称/左大小/右大小/状态（仅左侧/仅右侧/类型不同/大小不同/相同）。
  - **内联标注**：链接期间文件列表行首细色条 + 悬停说明（仅左=琥珀、仅右=天蓝、不同=红），不改列结构；表头「名称」旁显示方位链接标识；标注仅在窗格正处比对层时显示。
- **比对后端能力抽象**：`diff.rs` 抽出纯函数 `compare_entries`（只吃两侧 `FileEntry`，与协议无关）；`list_dir` 命令的协议前缀路由提取为共享 `list_side`，`diff_dirs` 复用同一调度；新增 `BackendKind`/能力表（Local=三层+可建目录、SFTP=二层+可建目录、HTTP=一层/不可建目录），比对层级按两侧能力**自动钳制**——任一侧为 SFTP 时最多比到大小且不进 hash 阶段。未来接入 S3/网盘 = 增加一个分类臂 + 一条能力条目 + 一个列目录分支，比对内核与 UI 零改动。
- **SFTP 比对限制说明**：SFTP v3 无远程 exec 通道、mtime 仅秒精度，故不比内容 hash 与修改时间；「在对侧新建并进入」是本特性唯一远程写入。
- 与 v0.17 的**差异比对窗口（模态）并存**：深比对（本地↔本地三层 hash + 同步复制/删除）仍走窗口，实时同层巡检（含 SFTP）走底部面板。

## v0.17.2 (2026-09-26)

- **地址栏 `~` 与相对路径**：地址栏输入 `~/.config/opencode/` 这类路径回车即定位到当前用户主目录下的目录。新增后端 `resolve_path`（展开 `~`/`~/`/`~\`、以当前窗格为基准解析相对路径、词法归一化 `.`/`..`），并修复目录补全在「以分隔符结尾」时不列子项、以及多级相对路径不以当前目录为基准的问题。
- **差异比对增强（P0）**：
  - **中途可停止 + 进度**：比对改为后台任务并推送 `diff-progress` 事件，窗口底部显示阶段、`已完成/总数`、百分比、进度条、当前项与「停止」按钮；关闭窗口自动取消。
  - **类型冲突**：同名但一侧目录一侧文件标记为「不同·类型」，不再误判为相同；同步时也能正确覆盖（修复 `copy_recursive` 目录/文件互覆盖）。
  - **同步安全**：同步前二次确认（列出覆盖/删除项数），逐项执行并汇总成功/失败，失败不中断；新增可展开、可复制的同步日志。
- **差异比对增强（P1）**：新增「只看差异」过滤、层级偏好持久化、名称匹配大小写开关（默认跟随平台）、三层时间容差（默认 2s）、窗口最大化/还原、双击差异项在对应窗格定位；窗格空白右键新增「与另一窗格差异比对…」入口。
- **差异比对说明**：当前仅支持**两个本地目录**；本地 ↔ SFTP/HTTP 比对与递归比对列入 P2（见路线图）。

## v0.17.1 (2026-09-25)

- **修复标签页自定义名丢失**：右键重命名标签页后，重启程序（即使「保存并退出」）自定义名会丢失。根因是后端 `SessionTab` 结构缺 `customTitle` 字段，serde 在写 `session.json` 时静默丢弃该字段；补齐字段后自定义名随会话往返。新增 Rust 回归测试。
- **空白处右键「属性（当前目录）」**：右键窗格空白区域新增属性项，统计**当前目录**（递归大小/文件数/文件夹数）。即使此前单击了某个文件、地址栏已显示该文件完整路径，空白处属性仍统计当前目录本身，不受选中项影响。
- 新增后端 `stat_entry` 命令（读取单路径元信息），复用属性弹窗与滚动数字。

## v0.17.0 (2026-09-25)

- **差异比对 + 同步**：菜单栏新增「工具 → 差异比对（左右窗格）…」，弹出专用比对窗口，比对**当前标签的两个本地窗格**（左右各一目录）。
  - **一层（默认）**：比对文件夹与文件名，列出「仅左 / 仅右 / 相同」。
  - **二层**：同名文件再比对内容大小。
  - **三层（可选）**：同名文件再比对内容 hash 与修改时间戳。
  - 支持多选差异后「向左同步 / 向右同步」：以另一侧为准，覆盖复制（目录递归）或删除目标侧独有条目，完成后自动刷新窗格。
  - 后端 `diff_dirs` 仅比对顶层、不递归；同步复用既有复制/删除命令（覆盖语义）。
- **修复**：`ops` 一个 Windows 路径分隔符断言（既有测试问题），改用 `PathBuf` 比较，使 `cargo test` 在本机也全绿。

## v0.16.0 (2026-09-25)

- **右键「属性」**：文件/文件夹右键新增「属性」，弹窗显示名称、类型、位置、大小、包含（文件数 · 文件夹数）、时间、权限。**文件夹递归统计大小**（迭代式 DFS，只读元数据、不跟随符号链接），统计过程按增量进度刷新，数字用**老虎机式逐位上下滚动**动画（`transform` 合成层，轻量；支持 `prefers-reduced-motion` 降级）。关闭弹窗即取消后台统计。
- **修复右键菜单溢出窗口**：界面字体缩放（根元素 CSS `zoom`）会让 Radix/Floating UI 的 portal 定位按 zoom 倍率整体偏移，右键菜单因此掉出窗口底部。对右键菜单 popper 做 zoom 反向补偿（包裹层 `zoom: 1/zoom` 对齐视口、内容再缩放回原大小、可用高度按 zoom 折算），并移除原先 zoom 无关的高度覆盖。仅作用于右键菜单。

## v0.15.0 (2026-09-25)

- **标签页右键菜单**：右键标签新增「关闭标签 / 重命名 / 移动到最右边」；仅剩一个标签时禁用「关闭标签」，已在最右时禁用「移动到最右边」。菜单改为容器内绝对定位（非 Portal），避免界面字体缩放（根 `zoom`）下溢出窗口。
- **修复窗格列表溢出**：路径栏右侧「已连接的路径（窗格列表）」下拉在界面字体缩放（根 `zoom`）下会溢出到窗口右侧、路径列被裁掉；改为非 Portal 的容器内绝对定位，右对齐贴窗口右缘。

## v0.14.0 (2026-09-25)

- **地址栏显示选中路径**：单击文件/文件夹时，地址栏直接显示其完整路径；取消选择或多选时回落当前目录。
- **标签配置写盘持久化**：标签重命名、文件标签、自定义快捷访问额外写入配置目录 `prefs.json`（与 `session.json` 同级），作为 WebView localStorage 的可靠备份，启动时合并恢复，避免本地存储被清理导致标签名丢失。
- **复制 / 剪切提示**：Ctrl+C / Ctrl+X 成功后窗口中央显示「已复制 / 已剪切」，约 1 秒后消失。
- **显示文件扩展名开关**：查看菜单新增「显示文件扩展名」，作用于文件列表与标签视图。
- **版本号同步测试**：新增测试校验 `package.json / tauri.conf.json / Cargo.toml` 三处版本彼此一致且等于最新 git tag。

## v0.13.0 (2026-09-23)

- **设置改为滑动页布局**：设置对话框（帮助 → 设置 / 快捷键）不再因为点击左栏子菜单而整体跳动——外观 / 快捷键 / 打开方式 / 分享 / 插件 / 关于 全部渲染在同一条可滚动内容列中，点左栏即锚点跳转到对应分区，滚动时左栏高亮自动跟随。
- **固定宽高**：对话框尺寸改为 `720 × min(600px, 85vh)`，切换分区时高度保持恒定，分区之间加分隔线便于定位。

## v0.12.0 (2026-09-21)

- **搜索结果右键菜单**：Ctrl+F 文件名/内容搜索结果支持右键「定位到文件/文件夹、打开（默认应用）、复制绝对路径、复制相对路径」；单击选中，**双击按扩展名用系统关联程序打开**（目录则进入）。修复递归搜索定位到搜索根目录而非文件父目录的问题。
- **标签视图右键菜单**：标签（虚拟文件夹）内的条目右键弹出应用内菜单（打开 / 用默认应用打开 / 定位 / 标签 / 复制路径 / 快捷访问 / 移除此标签），不再出现 WebView 默认右键菜单；新增批量类型探测，正确区分标签内的文件夹与文件。
- **Windows 应用名识别**：窗口/网页标题统一为 `R-Dir`，补全可执行文件版本元数据（publisher / 描述 / 版权），并在 Windows 启动时设置 AppUserModelID，使任务栏正确分组与显示应用名。
- **应用内更新**：帮助菜单新增「检查更新…」「GitHub 仓库」；基于 GitHub Releases API 比对最新版本，有新版时引导前往下载页；启动后静默检查一次。
- **设置内显示版本**：设置对话框新增「关于」分区，显示应用名与版本（取自应用元数据，随发布 tag 同步），底部状态栏显示 `R-Dir vX.Y.Z`。
- **发布流程**：打 tag 时自动把 `vX.Y.Z` 写入 `package.json / tauri.conf.json / Cargo.toml`（版本与 tag 一致）；Release 说明自动带上本文件的变更段落；**Windows 只发布 NSIS `-setup.exe`**，避免 MSI 与 NSIS 混装导致的升级报错「Error launching installer」。
- **内部重构**：移除未使用的 shadcn 组件与死代码，抽取 `useSftp`，补齐测试基建（vitest + Playwright E2E + Rust）。
