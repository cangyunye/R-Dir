# R-Dir 回归测试执行手册

> 基于 TEST_SCENARIOS.md 的可执行回归用例。
> 每次发版前执行 P0 全部 + P1 抽样。P2 按需。

---

## 环境准备

### 必备
- Windows：开发机或干净虚拟机，R-Dir 安装包
- WSL：开启远程登录（ssh localhost 已配好密钥或密码）
- nginx：`http://localhost:8082/` 指向一个含 ≥50 文件的目录
- 测试目录：`F:\rdir-test\`，含：
  - `empty/`（空目录）
  - `small/`（10 个文件）
  - `large/`（≥2000 个空文件）
  - `hidden/.dotfile`
  - `docs/readme.md`（含关键词"R-Dir"）

### 准备步骤
1. 卸载旧版 R-Dir，或清 localStorage（DevTools → Application → Local Storage → Clear）
2. 安装新版
3. 准备好 WSL ssh 连接信息
4. 确认 nginx 在 8082 端口运行

---

## P0 回归用例（阻塞发版）

### W1 启动无黑窗
| 项 | 内容 |
|---|---|
| 前置 | 无 |
| 步骤 | 双击 R-Dir 图标启动 |
| 预期 | 浅色窗口直接出现，无黑色闪烁 |
| 通过标准 | 无黑色帧 |

### W3 默认目录加载
| 项 | 内容 |
|---|---|
| 前置 | 首次启动 |
| 步骤 | 启动后等 3 秒 |
| 预期 | 文件列表非空，显示主目录内容 |
| 通过标准 | 至少看到 10 行文件 |

### W4 字体大小
| 项 | 内容 |
|---|---|
| 前置 | 清 localStorage 后启动 |
| 步骤 | 观察界面字体 |
| 预期 | 字体为 15px，不缩小 |
| 通过标准 | 文件名字体清晰，不小于系统默认 |

### W5 老用户字体迁移
| 项 | 内容 |
|---|---|
| 前置 | localStorage 设 `rfm.ui-font=13` |
| 步骤 | 启动 |
| 预期 | 自动迁移到 15px |
| 通过标准 | 字体不是 13px 的小字 |

### W6 菜单栏 hover
| 项 | 内容 |
|---|---|
| 前置 | 启动后 |
| 步骤 | 1. 点"文件"菜单 2. 鼠标移到"编辑" |
| 预期 | "编辑"菜单自动展开，不需要再点 |
| 通过标准 | 两个菜单都展开 |

### T1 新建标签
| 项 | 内容 |
|---|---|
| 前置 | 启动后 |
| 步骤 | 点标签栏 ＋ |
| 预期 | 新标签出现，显示默认目录 |
| 通过标准 | 标签数 +1 |

### T2 关闭标签
| 项 | 内容 |
|---|---|
| 前置 | 有 ≥2 个标签 |
| 步骤 | 中键点击非活动标签 |
| 预期 | 标签关闭 |
| 通过标准 | 标签数 -1 |

### N1 Backspace 返回
| 项 | 内容 |
|---|---|
| 前置 | 进入 `F:\rdir-test\large` |
| 步骤 | 按 Backspace |
| 预期 | 返回 `F:\rdir-test` |
| 通过标准 | 地址栏显示上级目录 |

### N2/N3 前进后退
| 项 | 内容 |
|---|---|
| 前置 | 从 large 返回后 |
| 步骤 | 点前进按钮 |
| 预期 | 回到 large |
| 通过标准 | 地址栏正确 |

### N5 地址栏跳转
| 项 | 内容 |
|---|---|
| 前置 | 任意目录 |
| 步骤 | 地址栏输 `F:\rdir-test` 回车 |
| 预期 | 进入该目录 |
| 通过标准 | 文件列表显示该目录内容 |

### N7 F5 不回初始目录
| 项 | 内容 |
|---|---|
| 前置 | 进入 `F:\rdir-test\large` |
| 步骤 | 按 F5 |
| 预期 | 只刷新当前窗格，不回到主目录 |
| 通过标准 | 地址栏仍为 large |

### L1 虚拟滚动完整
| 项 | 内容 |
|---|---|
| 前置 | 进入 large（≥2000 文件） |
| 步骤 | 不滚动，看首屏 |
| 预期 | 首屏 ≥15 行可见文件 |
| 通过标准 | 不是只显示 6 行 |

### L3 矩形框选
| 项 | 内容 |
|---|---|
| 前置 | 任意目录 |
| 步骤 | 左键拖拽出矩形框住 5 个文件 |
| 预期 | 框中的文件高亮选中 |
| 通过标准 | 5 个文件都选中 |

### L4 Shift 多选
| 项 | 内容 |
|---|---|
| 前置 | 任意目录 |
| 步骤 | 点第 1 个文件，Shift 点第 10 个 |
| 预期 | 连续选中 10 个 |
| 通过标准 | 10 个文件都选中 |

### L6 慢速双击重命名
| 项 | 内容 |
|---|---|
| 前置 | 任意文件 |
| 步骤 | 慢速点文件名两次（间隔 >500ms） |
| 预期 | 进入重命名输入框 |
| 通过标准 | 文件名变成可编辑文本框 |

### L7 右键重命名
| 项 | 内容 |
|---|---|
| 前置 | 任意文件 |
| 步骤 | 右键→重命名 |
| 预期 | 进入重命名输入框 |
| 通过标准 | 同上 |

### L8 Delete 删除
| 项 | 内容 |
|---|---|
| 前置 | 准备一个测试文件 |
| 步骤 | 选中按 Delete（文件夹需确认） |
| 预期 | 文件删除（文件夹弹确认对话框） |
| 通过标准 | 文件消失 |

### L10 隐藏文件开关
| 项 | 内容 |
|---|---|
| 前置 | 进入含 `.dotfile` 的目录 |
| 步骤 | 开/关"显示隐藏" |
| 预期 | `.dotfile` 出现/消失 |
| 通过标准 | 正确切换 |

### F1/F2 复制剪切粘贴
| 项 | 内容 |
|---|---|
| 前置 | 在 small 目录选 2 个文件 |
| 步骤 | Ctrl+C，到 empty 目录 Ctrl+V |
| 预期 | 文件复制过去 |
| 通过标准 | empty 里有 2 个文件 |

### F3/F4 新建文件夹/文件
| 项 | 内容 |
|---|---|
| 前置 | 在 empty 目录空白处右键 |
| 步骤 | 新建文件夹 → 直接输入名；新建文本文件 → 直接输入名 |
| 预期 | 新建项出现，直接进入重命名状态 |
| 通过标准 | 不需要再右键重命名 |

### F5/F6/F7 拖拽
| 项 | 内容 |
|---|---|
| 前置 | 左右分屏，左本地右 SFTP |
| 步骤 | 拖本地文件到 SFTP |
| 预期 | 上传成功，不报"文件不存在" |
| 通过标准 | SFTP 目录出现该文件 |

### C1 右键文件菜单
| 项 | 内容 |
|---|---|
| 前置 | 右键文件 |
| 步骤 | 观察菜单项 |
| 预期 | 打开/重命名/复制/剪切/删除/压缩/标签/终端 |
| 通过标准 | 菜单平铺，不折叠 |

### C2 右键空白菜单
| 项 | 内容 |
|---|---|
| 前置 | 右键空白处 |
| 步骤 | 观察菜单项 |
| 预期 | 新建文件夹/新建文件相邻，终端，刷新 |
| 通过标准 | 新建两项相邻 |

### C7 右键标签
| 项 | 内容 |
|---|---|
| 前置 | 右键文件→标签→红 |
| 步骤 | 观察文件左边 |
| 预期 | 出现红色小圆点 |
| 通过标准 | 红点可见 |

### TG1/TG2 标签虚拟目录
| 项 | 内容 |
|---|---|
| 前置 | 文件已加红色标签 |
| 步骤 | 1. 左侧点红色标签 2. 观察面板 3. 点"退出" |
| 预期 | 标签面板打开，列出文件；点退出能关闭，回到之前目录 |
| 通过标准 | 退出按钮有效，不卡死 |

### S1 密码连接
| 项 | 内容 |
|---|---|
| 前置 | WSL ssh 已开启 |
| 步骤 | 地址栏 `sftp://user@localhost:22`，弹框输密码 |
| 预期 | 连接成功，列出远程目录 |
| 通过标准 | 远程文件显示 |

### S6 主密码验证
| 项 | 内容 |
|---|---|
| 前置 | 已保存密码的服务器 |
| 步骤 | 重连该服务器 |
| 预期 | 要求输主密码 |
| 通过标准 | 弹出主密码框 |

### S10 已连接后刷新
| 项 | 内容 |
|---|---|
| 前置 | SFTP 已连接 |
| 步骤 | 点左侧服务器 |
| 预期 | 目录正确加载（不是本地内容） |
| 通过标准 | 显示远程目录 |

### H1 HTML autoindex
| 项 | 内容 |
|---|---|
| 前置 | nginx 8082 运行中 |
| 步骤 | 地址栏 `http://localhost:8082/` |
| 预期 | 列出目录内容 |
| 通过标准 | 文件列表显示 |

### H3/H5 导航+拖拽
| 项 | 内容 |
|---|---|
| 前置 | 在 autoindex 目录 |
| 步骤 | 1. 点子目录 2. 拖文件到本地窗格 |
| 预期 | 进入子目录；文件下载到本地 |
| 通过标准 | 下载有进度条 |

---

## P1 回归用例（重要，抽样）

### 标签页
- T3 拖拽排序
- T4 Ctrl+Tab 切换
- T6 中键新标签

### 导航
- N4 鼠标侧键前进后退
- N6 地址栏补全
- N8/N9 Ctrl+J / Ctrl+\ 分屏

### 文件列表
- L2 滚动流畅
- L5 单击选中
- L9 键盘定位（2 秒重置）
- L11 Ctrl 多选

### 文件操作
- F8 拖拽进度条
- F9 拖拽后自动刷新

### 右键菜单
- C3 新建项相邻
- C4 终端菜单项
- C5 菜单不越界
- C6 压缩
- C8 快捷访问

### SFTP
- S2 密钥自动找 ~/.ssh/id_*
- S3/S4 路径栏 sftp://
- S5 主密码设置
- S8 会话恢复
- S9 未验证主密码
- S11 删除

### HTTP
- H2 JSON autoindex
- H4 点击文件下载
- H6 断点续传

### 快捷键
- KB1 Enter 打开
- KB3 Ctrl+Tab
- KB4 Ctrl+J
- KB6 Ctrl+C/X/V
- KB7 F5
- KB8 Ctrl+滚轮缩放

---

## P2 回归用例（次要）

- W2 开屏动画视觉
- W8 会话恢复完整布局
- T5 标签右键菜单
- SE1-SE12 搜索文件/内容全部
- SH1-SH6 分享插件
- CP1-CP4 压缩
- S12 SFTP 分组

---

## 自动化用例（Playwright，`pnpm test:e2e`）

实际落地在 `e2e/smoke.spec.ts` + `e2e/tauri-mock.ts`。Tauri IPC 用
`addInitScript` 注入的桩替代，配固定虚拟文件系统 `/mock/home`
（Documents/ Projects/ Notes.txt/ photo.png/ link-to-docs）。
首次运行需 `pnpm exec playwright install chromium`。

| 用例 | 对应场景 | 自动化方式 |
|---|---|---|
| W1 默认打开家目录并列出内容 | W1/W3 | 断言地址栏 = /mock/home + 5 行 data-path |
| W2 目录排在文件前面 | L1 | 断言行序：目录在前、文件在后 |
| W6 菜单栏包含 文件/编辑/查看 | W6 | 断言菜单栏文本 |
| N1 双击目录进入下一级 | N1 | 断言地址栏与子目录内容 |
| N2 返回上一级 | N1 | 平台键位（mac ⌘↑ / win Alt↑），断言地址栏回退 |
| N7 F5 刷新后列表仍在 | N7 | 断言刷新后条目仍可见 |
| L1 单击选中一行 | L1 | 断言行获得选中样式 |
| L2 全选 | L2 | 断言 5 行全部选中 |
| T1 新开标签页 | T1 | 断言标签数 1 → 2 |
| C1 右键文件出现操作项 | L7 | 断言菜单含 打开/重命名/删除 |
| C2 Escape 关闭右键菜单 | L7 | 断言菜单消失 |
| S1 打开搜索面板 | SE1 | 断言搜索输入框出现 |
| 全量快捷键冒烟 ×17 | — | 按键后断言数据仍在、可继续操作 |

**仍需人工**（真 Tauri 窗口或外部服务）：真窗口无黑窗、上千文件虚拟滚动、
矩形框选、SFTP 真连接/上传、nginx autoindex、拖拽与进度条视觉。

---

## 执行记录

| 版本 | 执行日期 | 执行人 | P0 通过 | P0 失败 | P1 抽样 | 备注 |
|---|---|---|---|---|---|---|
| v0.8.0 | | | | | | |
